{
  "name": "nafa-audit-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker — syncs NAFA audit files from Monday.com to Close CRM",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
